import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.extramega1stack.goldengods',
  appName: 'Golden Gods',
  webDir: 'dist',
  android: {
    backgroundColor: '#0f0e13',
  },
};

export default config;
